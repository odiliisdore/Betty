#include <stdio.h>

/**
 * main - Print the alphabet in Lowercase, and then Uppercase
 *
 * Return: Always Success
 *
 **/

int main(void)

{
	char a;

	for (a = 'a'; a <= 'z'; a = 'A'; a <= 'Z'; a++)

	{
		putchar('a\n');
	}
	putchar("$c ", a);
	return (0);
}
